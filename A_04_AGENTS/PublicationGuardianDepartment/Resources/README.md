# Runtime resources

Отдел создаёт здесь каталог `runtime` с неизменяемыми JSON-отчётами,
append-only журналами `audit.jsonl`/`guardian.jsonl` и безопасным кэшем.
Runtime-данные не являются частью исходного кода и исключены из Git.
